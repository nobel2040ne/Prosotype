# auto-CWI — Python 3.11 (macOS arm64 / Linux). All inference is local.
numpy==1.26.4
PyYAML==6.0.2
pydantic==2.9.2

# ASR
faster-whisper==1.1.1
ctranslate2==4.5.0
transformers==4.48.3
# From master, not PyPI: the `multi` bilingual profile needs PR #3671, which no
# release contains. Pin back to 1.13.4 to revert — only that profile breaks.
sherpa-onnx @ git+https://github.com/k2-fsa/sherpa-onnx@master  # -> 1.13.5

# Diarization — pyannote weights are gated; see CONTRIBUTING.md for HF_TOKEN
torch==2.5.1
torchaudio==2.5.1
pyannote.audio==3.3.2

# Prosody
librosa==0.10.2.post1
soundfile==0.12.1
praat-parselmouth==0.4.5

# Capture, rendering, probes, tests
sounddevice==0.5.1
Pillow==11.0.0
websockets==17.0
pytest==8.3.4
